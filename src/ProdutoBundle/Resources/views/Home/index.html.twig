{% extends 'ProdutoBundle::layout.html.twig' %}

{% block content_body %}
    <div class="container-fluid text-center">
        <div class="row">
            <div role="main" class="col-md-6 col-md-push-3">
                <div class="row">
                    &nbsp;
                </div>
                <div class="row">
                    <a href="{{ path('cadastro') }}" class="btn btn-primary btn-lg btn-block active" role="button">Cadastrar Produto</a>
                </div>
                <div class="row">
                    &nbsp;
                </div>
                <div class="row">
                    <a href="{{ path('estatisticas') }}" class="btn btn-primary btn-lg btn-block active" role="button">Estatisticas dos Produtos</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

